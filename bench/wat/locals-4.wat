;; locals-4 — generated by bench/gen.py; edit the generator, not this file.
(module
  (func (export "run") (result i32)
    (local $i i32) (local $acc i32) (local $pad0 i32) (local $pad1 i32) (local $hot i32)
    block $done
      loop $next
        local.get $i
        i32.const 1000000
        i32.ge_u
        br_if $done
        local.get $hot
        local.get $i
        i32.add
        local.set $hot
        local.get $i
        i32.const 1
        i32.add
        local.set $i
        br $next
      end
    end
    local.get $hot))
